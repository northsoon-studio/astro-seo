import { defineConfig } from "astro/config";

// Minimal Astro config for editor/language-server support in development.
// This library does not build an Astro app — config is dev-only.
export default defineConfig({});
